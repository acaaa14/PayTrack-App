<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TAGIHAN123"
        tools:targetApi="31">


        <!-- LOGIN sebagai halaman pertama -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Register -->
        <activity android:name=".ui.RegisterActivity" />

        <!-- Main Menu -->
        <activity android:name=".ui.MainActivity" />

        <!-- Profil -->
        <activity android:name=".ui.ProfileActivity" />

        <!-- Tambah Tagihan -->
        <activity android:name=".ui.AddBillActivity" />

        <!-- Daftar Tagihan -->
        <activity android:name=".ui.ListBillsActivity" />
        <activity android:name=".ui.EditProfileActivity" />
        <activity android:name=".ui.ChangePasswordActivity" />

        <!-- BroadcastReceiver Notifikasi -->
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver untuk Cek Tagihan Harian -->
        <receiver
            android:name=".receiver.DailyCheckReceiver"
            android:exported="false" />

    </application>

</manifest>
